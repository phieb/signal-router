WEBHOOK_URLS=http://n8n:5678/webhook/signal
WEBHOOK_SECRET=
ALLOWLIST_SENDERS=false
API_KEY=
LOG_LEVEL=INFO

# Only needed for `make register` / `make verify` (registering a new number).
# The router itself auto-discovers the phone from signal-cli.
SIGNAL_PHONE_NUMBER=+43XXXXXXXXX
